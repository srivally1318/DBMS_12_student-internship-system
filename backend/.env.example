# Web server (Express). Local: 5000. On Render, do not set — Render provides PORT.
PORT=5000

# Aiven MySQL — connection port is NOT the same as PORT above
DB_HOST=your-aiven-mysql-host.aivencloud.com
DB_USER=avnadmin
DB_PASSWORD=your_aiven_password
DB_NAME=defaultdb
DB_PORT=27046
